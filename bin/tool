#!/usr/bin/env node

require('commander')
    .version(require('../package').version)
    .usage('<command> [options]')
    .command('init', 'generate a new page from template')
    .command('list', 'list all page in web')
    .parse(process.argv)